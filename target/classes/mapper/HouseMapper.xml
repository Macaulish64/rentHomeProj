<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- User的ORM-->
<mapper namespace="com.rent.dao.HouseDao">
  <!-- 目的：为dao接口方法提供sql语句配置 -->
  <!-- 具体的sql -->
  <insert id="addHouse">
      insert into house(
                        publishUserId,cityName,communityName,buildingNumber,houseType,
                        houseArea,floorNumber,elevatorOrNot,houseDescription,
                        housePhoto,deposit,paymentMethod,rent,
                        registTime,updateTime,housestatus)
          values (
                  #{publishUserId},#{cityName},
                  #{communityName},#{buildingNumber},#{houseType},
                  #{houseArea},#{floorNumber},#{elevatorOrNot},
                  #{houseDescription},#{housePhoto},
                  #{deposit},#{paymentMethod},
                  #{rent},#{registTime},
                  #{updateTime},#{housestatus}
          );
  </insert>



</mapper>